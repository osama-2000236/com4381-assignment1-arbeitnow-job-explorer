# Presentation Checklist

- Open the GitHub repository
- Open `README.md`
- Open the Postman collection
- Open the live API endpoint
- Open the running app
- Switch the live request to `visa_sponsorship=true`
- Show one successful search
- Show one filter change
- Show one job detail view
- Show the original application link
- Mention the REST resource, method, JSON response, `page` query parameter, and `visa_sponsorship` query parameter
